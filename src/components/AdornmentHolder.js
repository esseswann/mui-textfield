import styled from '@emotion/styled/macro'

const AdornmentHolder = styled.div({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  flexSrhink: 0,
  minWidth: 48
})

export default AdornmentHolder